import RatesettingModel from "../../model/masters/RatesettingModel";
export const createRatesetting = async (req, res) => {
    try {
const {}
    } catch (error) {
        console.log("error:", error.message)
        return res.status(500).json({ message: "Internal server error" })
    }
}